{% extends 'baseoffline.html.twig' %}
{% block body %}
    <form class="m-t" role="form" action="{{ path('app_reset_password') }}" method="post">
        <img src="/img/mapsi.png" class="rounded mx-auto d-block">
        <br>
        <h2 class="text-center"><strong>Réinitialiser votre mot de passe</strong></h2>

        <br><br>

        {{ form_start(resetForm) }}
            {{ form_row(resetForm.plainPassword) }}
            <button class="btn btn-primary">Réinitialiser votre mot de passe</button>
        {{ form_end(resetForm) }}
    </form>
{% endblock %}


